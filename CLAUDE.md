# Claude Code Project Notes

## Git Remote
- Always push to the `ny4i` remote (`git@github.com:ny4i/n1mm_view.git`).
- Never use `origin`. The origin remote has been removed to prevent accidental pushes to the upstream n1kdo repo.
